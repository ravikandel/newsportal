$(document).ready(function () {
    var acookie = ReadCookie("theme");
    if (acookie) {
        $("#kbpatra-body").removeClass(), $("#kbpatra-body").addClass(acookie);
    }
});

function changeTheme(e) {
    WriteCookie(e);
}
// Write Cookie
function WriteCookie(e) {
    var now = new Date();
    now.setDate(now.getDate() + 1);
    var expires = "expires=" + now.toUTCString();
    document.cookie = "theme=" + e + ";" + expires + ";path=/";
    var acookie = ReadCookie("theme");
    if (acookie) {
        $("#kbpatra-body").removeClass(), $("#kbpatra-body").addClass(acookie);
    }
}

function ReadCookie(c_name) {
    var c_value = document.cookie,
        c_start = c_value.indexOf(" " + c_name + "=");
    if (c_start == -1) c_start = c_value.indexOf(c_name + "=");
    if (c_start == -1) {
        c_value = null;
    } else {
        c_start = c_value.indexOf("=", c_start) + 1;
        var c_end = c_value.indexOf(";", c_start);
        if (c_end == -1) {
            c_end = c_value.length;
        }
        c_value = unescape(c_value.substring(c_start, c_end));
    }
    return c_value;
}

function switchTab(evt, tabName) {
    var i, tabcontent, tablinks;
    tabcontent = document.getElementsByClassName("tabcontent");
    for (i = 0; i < tabcontent.length; i++) {
        tabcontent[i].style.display = "none";
    }
    tablinks = document.getElementsByClassName("tablinks");
    for (i = 0; i < tablinks.length; i++) {
        tablinks[i].className = tablinks[i].className.replace(" active", "");
    }
    document.getElementById(tabName).style.display = "block";
    evt.currentTarget.className += " active";
}

function gtag() {
    dataLayer.push(arguments)
} ! function (e) {
    e("#stickable-css").length || e("head").append('<style id="stickable-css">.stick {margin-top:0!important;position:fixed;top: 0;z-index:10000;}</style'), e.fn.stickable = function () {
        return this.each(function () {
            var t = e(this),
                r = t.outerHeight();
            t.outerHeight(r).before("<div />");
            var o = t.prev();
            e(window).on("scroll", function () {
                var a = e(this).scrollTop(),
                    s = o.offset().top,
                    n = t.hasClass("stick");
                a > s && !n && (t.width(t.width()).addClass("stick"), o.height(r)), a < s && (t.removeClass("stick"), o.height(0))
            }).trigger("scroll"), e(window).on("resize", function () {
                t.hasClass("stick") ? (t.removeClass("stick"), t.outerWidth(o.width()), t.addClass("stick")) : t.css({
                    width: ""
                })
            }).trigger("resize")
        })
    }
}(jQuery), jQuery(function () {
    (new WOW).init()
}), jQuery(document).ready(function (e) {
    e("#sticky").stickable()
}), jQuery(document).ready(function () {
    jQuery(window).scroll(function () {
        jQuery(this).scrollTop() > 50 ? jQuery("#back-to-top").fadeIn() : jQuery("#back-to-top").fadeOut()
    }), jQuery("#back-to-top").click(function () {
        return jQuery("#back-to-top").tooltip("hide"), jQuery("body,html").animate({
            scrollTop: 0
        }, 800), !1
    })
}),
    jQuery(document).ready(function () {
        jQuery('#list').click(function (event) { event.preventDefault(); $('#news .item').removeClass('grid-group-item'); $('#news .item').addClass('list-group-item');$('#news .it').removeClass('img_wrap5'); });
        jQuery('#grid').click(function (event) { event.preventDefault(); $('#news .item').removeClass('list-group-item'); $('#news .item').addClass('grid-group-item');$('#news .it').addClass('img_wrap5'); });
	
	jQuery('#A').click(function (event) { 
		event.preventDefault(); 
			$('#newsContent > p').css({ 'font-size': '1.9rem' });
			$('#A').removeClass().addClass('btn btn-lg btn-primary'); 
			$('#AA').removeClass().addClass('btn btn-lg btn-outline-primary'); 
			$('#AAA').removeClass().addClass('btn btn-lg btn-outline-primary'); 
		});
	jQuery('#AA').click(function (event) { 
		event.preventDefault(); 
			$('#newsContent > p').css({ 'font-size': '2.2rem' });
			$('#AA').removeClass().addClass('btn btn-lg btn-primary'); 
			$('#A').removeClass().addClass('btn btn-lg btn-outline-primary'); 
			$('#AAA').removeClass().addClass('btn btn-lg btn-outline-primary'); 
		});
	jQuery('#AAA').click(function (event) { 
		event.preventDefault(); 
		    $('#newsContent > p').css({ 'font-size': '2.5rem' });
			$('#AAA').removeClass().addClass('btn btn-lg btn-primary'); 
			$('#A').removeClass().addClass('btn btn-lg btn-outline-primary'); 
			$('#AA').removeClass().addClass('btn btn-lg btn-outline-primary'); 
		});
    }),
    jQuery(function () {
        jQuery(".search-toggle").length && jQuery(".search-toggle").on("click", function () {
            jQuery(this).toggleClass("active"), jQuery(this).next(".search-box").toggleClass("now-visible")
        })
    }), jQuery("#navigation1").length > 0 && jQuery("#navigation1").navigation({
        effect: "slide"
    }), window.dataLayer = window.dataLayer || [], gtag("js", new Date), gtag("config", "UA-168374640-1");